import 'package:flutter/material.dart';

class NamingAromaticPage extends StatelessWidget {
  const NamingAromaticPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
