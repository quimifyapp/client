import 'package:flutter/material.dart';

class NamingCyclicPage extends StatelessWidget {
  const NamingCyclicPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
